# VoidCat-DSN v2.0 Environment Configuration
# Copy this file to .env and customize as needed

# Anthropic API Key (REQUIRED)
ANTHROPIC_API_KEY=your-api-key-here

# External Port Mappings (Optional - defaults shown)
# Change these if the default ports are already in use on your system
OMEGA_PORT=3000
BETA_PORT=3002
GAMMA_PORT=3003
DELTA_PORT=3004
SIGMA_PORT=3005

# Note: Internal container port is always 3001
# These variables only affect the host → container port mapping
